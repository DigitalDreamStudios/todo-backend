version: '3'
services:
  backend:
    image: todo-backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3002:3000
    container_name: todo-backend
    env_file:
      - .env.production
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
